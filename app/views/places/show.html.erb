<div class="container all-show">
  <div class="bookmark-div">
    <%= link_to new_place_bookmark_path(@place), class: "bookmark" do %>
      <i class="fa fa-bookmark"></i>
    <% end %>
  </div>


  <div>
    <h2><strong><%= @place.name  %></strong></h2>
  </div>

  <div>
    <h5><%= @place.address %></h5>
  </div>

  <div>
    <h5>Rating:</h5>
    <p><%= @place.ratings %> <%= @place.total_ratings %> Reviews</p>
    <p></p>
  </div>

  <div>
    <i class="fa fa-star"></i>
    <i class="fa fa-star"></i>
    <i class="fa fa-star"></i>
    <i class="fa fa-star"></i>
    <i class="fa fa-star"></i>
  </div>

  <div class = "carosal">
  <input type="radio" name="position" class="circle" checked/>
  <input type="radio" name="position" class="circle" />
  <input type="radio" name="position" class="circle" />
  <input type="radio" name="position" class="circle" />
  <input type="radio" name="position" class="circle" />
  <main id="carousel">
      <button id="prev-button">Prev</button>
    <div class="item">
    <%= image_tag place_photo_path(@place.photos[1]), class: "photo-caro"%>
    </div>
      <div class="item">
    <%= image_tag place_photo_path(@place.photos[2]), class: "photo-caro"%>
    </div>
      <div class="item">
    <%= image_tag place_photo_path(@place.photos[3]), class: "photo-caro"%>
    </div>
      <div class="item">
    <%= image_tag place_photo_path(@place.photos[4]), class: "photo-caro"%>
    </div>
      <div class="item">
    <%= image_tag place_photo_path(@place.photos[5]), class: "photo-caro"%>
    </div>
        <button id="next-button">Next</button>
    <main>
 </div>


    <%# <div class="reqtangle">
    <% @place.photos.each do |photo_ref| %>
        <%# <%= image_tag place_photo_path(photo_ref), class: "reqtanglePhoto" %>
      <%# <% end %>
    <%# </div> %>
  <%# </div> %>

    <div class="bottom-buttons-div">
    <a class= "bottom-buttons" href="www.google.com">Open in maps</a>
    <%= link_to "Randomise Again", place_path, class: "bottom-buttons" %>
    </div>
  </div>
</div>

<div class="container bottom-div-all">
  <div class="row">
    <div class="col bottom-div">
          <%= link_to bookmarks_path, class: "bookmark bottom-links" do %>
        <i class="fa fa-bookmark"></i>
      <% end %>
    </div>

    <div class="col bottom-div">
      <%= link_to raw('<i class="fa fa-home"></i>'), root_path, class: "bookmark bottom-links" %>
    </div>
  </div>
</div>

<script>
const prevButton = document.querySelector('#prev-button');
const nextButton = document.querySelector('#next-button');
const radioButtons = document.querySelectorAll('.carosal input[type="radio"]');

let checkedIndex = 0;

// Find the initially checked radio button index
radioButtons.forEach((radioButton, index) => {
  if (radioButton.checked) {
    checkedIndex = index;
  }
});

// Handle the previous button click
prevButton.addEventListener('click', () => {
  if (checkedIndex > 0) {
    radioButtons[checkedIndex - 1].checked = true;
    checkedIndex--;
  }
});

// Handle the next button click
nextButton.addEventListener('click', () => {
  if (checkedIndex < radioButtons.length - 1) {
    radioButtons[checkedIndex + 1].checked = true;
    checkedIndex++;
  }
});
</script>
